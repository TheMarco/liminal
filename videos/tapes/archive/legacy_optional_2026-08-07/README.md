# Archived optional VCR pool

These 34 OGV recordings were the active optional/short pool before the
`newshorts/` replacement on 2026-08-07. They are intentionally preserved for
possible later use.

`VhsTapeLibrary` scans only the top level of `videos/tapes/`, so nothing in
this directory can enter the active objective or optional tape pools.
